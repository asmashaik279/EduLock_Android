<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ECEFF1">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:background="@drawable/profile_gradient">

            <ImageView
                android:id="@+id/imgProfile"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:layout_centerInParent="true"
                android:src="@drawable/ic_profile"
                android:background="@drawable/circle_white_bg"
                android:padding="8dp" />

            <TextView
                android:id="@+id/txtEmailTop"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/imgProfile"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="8dp"
                android:text="email@gmail.com"
                android:textColor="#FFFFFF"
                android:textSize="14sp" />
        </RelativeLayout>

        <!-- Card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:orientation="vertical"
            android:background="@drawable/card_bg"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Account Information"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#263238"
                android:layout_marginBottom="12dp" />

            <include layout="@layout/profile_item_name" />
            <include layout="@layout/profile_item_mobile" />
            <include layout="@layout/profile_item_email" />
            <include layout="@layout/profile_item_address" />
            <include layout="@layout/profile_item_dob" />

        </LinearLayout>

        <Button
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="Save Profile"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:backgroundTint="#512DA8" />
    </LinearLayout>
</ScrollView>
